# MyReactApp — Real-time Button Press Counter

Simple React app showing a button press counter (updates immediately on press).

## Setup

Install dependencies:

```bash
npm install
```

Run the dev server:

```bash
npm run dev
```






Build for production:

```bash
npm run build
```

Preview the production build:

```bash
npm run preview
```
